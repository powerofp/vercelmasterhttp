# Core (no external dependencies for basic modes)
# Python 3.10+ required

# Optional: MITM interception (vercel_edge mode)
cryptography>=41.0.0

# Optional: HTTP/2 multiplexing (faster vercel_edge relay)
h2>=4.1.0
